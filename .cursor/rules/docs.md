# Cursor Documentation Rules

> Starter stub. Replace with project-specific documentation conventions after forking.

- Keep docs aligned with the canonical source files.
- Avoid duplicating workflow guidance across multiple docs.
- Follow `AGENTS.md` and `agent-os/tool-boundaries.md` before using tools.