# Cursor Frontend Rules

> Starter stub. Replace with project-specific frontend conventions after forking.

- Prefer minimal, testable UI changes.
- Keep visual examples in `examples/` when they are illustrative only.
- Follow `AGENTS.md` and `agent-os/tool-boundaries.md` before using tools.