# Cursor Orchestration Rules

> Starter stub. Replace with project-specific multi-agent orchestration conventions after forking.

- Keep handoffs explicit.
- Use task files as the source of truth.
- Follow `AGENTS.md` and `agent-os/tool-boundaries.md` before using tools.