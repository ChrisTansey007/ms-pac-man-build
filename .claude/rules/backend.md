# Backend Rules for [PROJECT_NAME]

This is a starter rule stub. Replace it with project-specific backend guidance after forking.

## Keep in mind
- Follow the canonical instructions in `AGENTS.md` first.
- Add service, API, database, and deployment constraints here if the project needs them.
- Leave intentional placeholders visible until the fork owner customizes this file.
